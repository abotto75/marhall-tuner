{
  "name": "marshall-tuner-v9.0.0",
  "version": "9.0.0",
  "scripts": {
    "build:presets": "node scripts/build_presets_from_csv.cjs public/data/presets.csv public/data/presets.json",
    "vercel-build": "npm run build:presets && echo Vercel build step complete"
  }
}